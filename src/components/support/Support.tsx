import styles from './support.module.scss'
import {AiOutlineMessage} from 'react-icons/ai'

export const Support = () => {
  return (
    <div className={styles.support}><AiOutlineMessage /></div>
  )
}
